name: build-and-release

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install build deps
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build single-file binary
        run: |
          pip install .
          pyinstaller --name uupdump --onefile scripts/entrypoint.py

      - name: Prepare artifact (raw binary)
        run: |
          TS=$(date -u +"%Y%m%d-%H%M%S")
          echo "TS=$TS" >> $GITHUB_ENV
          cp dist/uupdump uupdump-linux-x86_64-${TS}
          chmod +x uupdump-linux-x86_64-${TS}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: uupdump-linux-x86_64-${{ env.TS }}
          path: uupdump-linux-x86_64-${{ env.TS }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ env.TS }}
          tag_name: ${{ env.TS }}
          target_commitish: ${{ github.sha }}
          files: uupdump-linux-x86_64-${{ env.TS }}
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}


